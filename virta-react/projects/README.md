# Projects Directory

This directory contains standalone projects that were used during development or serve as reference implementations.

## Projects

### 📊 Dashboard (`dashboard/`)
A React + TypeScript dashboard project with:
- Sidebar navigation
- Card hover effects
- Tailwind CSS styling
- shadcn/ui components

**Setup:**
```bash
cd dashboard
npm install
npm run dev
```

### 🏆 Leaderboard (`leaderboard/`)
A standalone leaderboard implementation with:
- HTML/CSS/JavaScript
- Student rankings
- Statistics display

**Usage:**
Open `leaderboard.html` in a browser.

### 🛒 Avatar Shop (`avatar-shop/`)
Avatar shop implementation with:
- HTML/CSS/JavaScript
- Coin system
- Avatar purchasing

**Usage:**
Open `avatar-shop.html` in a browser.

### 👨‍🏫 Instructor (`instructor/`)
Instructor dashboard implementation with:
- HTML/CSS/JavaScript
- Assignment management
- Student overview

**Usage:**
Open `instructor.html` in a browser.

## Note

These projects are standalone implementations and are not integrated into the main VirTA application. They serve as reference or were used during the initial development phase.

